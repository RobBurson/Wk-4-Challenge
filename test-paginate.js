{/* <main>
<div class="list" id="list"></div>
<div class="pgNum" id="pagination"></div>
</main> */}

// const questions = ["Items for pagination here"];

// const questionElement = document.getElementById('item ID here');
// pageNumberElement = document.getElementById('pagination');
//let currentPage = 1;
//let pages = 4;

//function displayQuiz (questions, wrapper, questionsPerPage, page) {
    //wrapper.innerHTML = "";
    //page--;

    //let begin = questionsPerPage * page;
    //let stop = start + questionsPerPage;
    //let paginatedItems = questions.slice(begin, end);
    //for (let i = 0; i < paginatedQuestions.length; i++) {
    // let questions = paginatedQuestions[i];

    // let questionElement = document.createElement('div');
    // questionElement.classList.add('question');
    // questionElement.innerText = question;

    // wrapper.appendChild(questionElement);
    //}
//}

//function setupPagination(questions, wrapper, questionsPerPage) {
    //wrapper.innerHTML = "";

    //let pageCount = Math.ceil(questions.length / questionsPerPage);

    //for (let i = 1; i < pageCount; i++;) {
        //paginationButton(i, questions);
    //}
//}

//function paginationButton(page, questions) {
    //let button = document.createElement('button');
    //button.innerText = next;

    //if(currentPage === page) button.classList.add('active');

    //return button;
//}

//displayQuiz(questions, questionElement, pages, currentPage);
//setupPagination(questionElement, pageNumberElement, questionsPerPage);